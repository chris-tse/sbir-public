<!DOCTYPE html>
<html lang="en">
<head>
    {{> head}}
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/profile.css">
    {{{_sections.head}}}
</head>
<body>
    {{> nav}}
    
    <div class="container" style="margin-top: 150px; margin-bottom: 50px">
        <div class="row">
            {{!-- <div class="col-sm-3">
                <div class="picplaceholder"></div>
            </div> --}}
            <div class="col-sm-9 align-items-center d-flex">
                <h1 class="name">{{name}}<br><small class="user-title"><em>{{userTitle}}</em></small></h1>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-3">
                <div class="nav flex-column nav-pills" id="v-pills-tab" aria-orientation="vertical">
                    <a class="nav-link {{#if profileTab}}active{{/if}}" href="/profile/{{username}}" id="profile-tab">User Profile</a>
                    <a class="nav-link {{#if listedTopics}}active{{/if}}" href="/profile/{{username}}/listedtopics" id="listed-topics-tab">Listed Topics</a>
                    {{#if ownProfile}}
                        <div style="padding: 0 15px">
                            <hr>
                        </div>
                        <a class="nav-link {{#if editProfile}}active{{/if}}" href="/profile/{{username}}/edit" id="edit-profile-tab">Edit Profile</a>
                        <a class="nav-link {{#if bookmarksTab}}active{{/if}}" href="/profile/{{username}}/bookmarks" id="bookmarked-topics-tab">Manage Bookmarks</a>
                    {{/if}}
                </div>
            </div>
            <div class="col-sm-9">
                {{{body}}}
            </div>
        </div>
    </div>
    
    {{> footer}}
    {{> libs}}
    {{{_sections.scripts}}}
</body>
</html>